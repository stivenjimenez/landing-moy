<section class="hero">
  <div class="hero-container">
    <div class="hero-content">
      <h1>Patrones a Medida que <span>Transforman</span> tu Producción</h1>
      <p>
        Diseños exclusivos creados por expertos mundiales. Ajuste perfecto,
        optimización de tela y soporte dedicado para resultados profesionales.
      </p>
      <div class="hero-buttons">
        <a href="#form" class="btn-primary">Solicitar Propuesta</a>
        <a href="#features" class="btn-secondary">Ver Beneficios</a>
      </div>
    </div>
    <div class="hero-visual">
      <img src="/images/hero.png" alt="Patrones MOY" class="hero-image" />
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    min-height: 100svh;
    display: flex;
    align-items: center;
    background: linear-gradient(135deg, var(--white) 0%, #fafaff 100%);
    padding: 0 2rem;
    position: relative;
  }

  .hero-container {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: center;
    width: 100%;
  }

  .hero-content h1 {
    font-size: 3.2rem;
    font-weight: 800;
    line-height: 1.15;
    margin-bottom: 2rem;
    color: var(--dark);
    letter-spacing: -0.02em;
  }

  .hero-content h1 span {
    color: var(--primary);
    position: relative;
  }

  .hero-content p {
    font-size: 1.2rem;
    color: var(--gray);
    margin-bottom: 2.5rem;
    line-height: 1.65;
    max-width: 90%;
  }

  .hero-buttons {
    display: flex;
    gap: 1.25rem;
    align-items: center;
  }

  .btn-primary {
    background: var(--primary);
    color: var(--white);
    padding: 1rem 2.5rem;
    border-radius: var(--border-radius);
    text-decoration: none;
    font-weight: 600;
    display: inline-block;
    transition: all 0.3s ease;
    box-shadow: var(--shadow-soft);
  }

  .btn-primary:hover {
    background: var(--primary-dark);
    transform: translateY(-1px);
    box-shadow: 0 8px 25px rgba(99, 102, 241, 0.25);
  }

  .btn-secondary {
    background: transparent;
    color: var(--primary);
    padding: 1rem 2.5rem;
    border: 2px solid var(--gray-lighter);
    border-radius: var(--border-radius);
    text-decoration: none;
    font-weight: 600;
    display: inline-block;
    transition: all 0.3s ease;
  }

  .btn-secondary:hover {
    background: var(--primary);
    color: var(--white);
    border-color: var(--primary);
    transform: translateY(-1px);
  }

  .hero-visual {
    position: relative;
    height: 450px;
    background: linear-gradient(
      135deg,
      var(--primary) 0%,
      var(--primary-dark) 100%
    );
    border-radius: var(--border-radius-large);
    overflow: hidden;
    box-shadow: var(--shadow-large);
  }

  .hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 20px;
  }

  .pattern-demo {
    position: absolute;
    inset: 20px;
    background: rgba(255, 255, 255, 0.9);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 4rem;
    font-weight: 900;
    color: var(--primary);
    opacity: 0.8;
    backdrop-filter: blur(5px);
  }

  /* Tablet */
  @media (max-width: 1024px) {
    .hero {
      padding: 80px 2rem 2rem;
    }

    .hero-container {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 3rem;
    }

    .hero-content h1 {
      font-size: 3rem;
    }

    .hero-content p {
      font-size: 1.1rem;
    }

    .hero-visual {
      height: 400px;
    }

    .hero-buttons {
      justify-content: center;
    }
  }

  /* Mobile */
  @media (max-width: 768px) {
    .hero {
      padding: 80px 1.5rem 2rem;
    }

    .hero-content h1 {
      font-size: 2.5rem;
    }

    .hero-content p {
      font-size: 1rem;
      margin-bottom: 1.5rem;
    }

    .hero-visual {
      height: 300px;
    }

    .hero-buttons {
      flex-direction: column;
      gap: 1rem;
    }

    .btn-primary,
    .btn-secondary {
      width: 100%;
      max-width: 300px;
      text-align: center;
    }
  }

  /* Small Mobile */
  @media (max-width: 480px) {
    .hero {
      padding: 80px 1rem 2rem;
    }

    .hero-content h1 {
      font-size: 2rem;
      margin-bottom: 1rem;
    }

    .hero-content p {
      font-size: 0.95rem;
    }

    .hero-visual {
      height: 250px;
    }

    .pattern-demo {
      inset: 15px;
      font-size: 2.5rem;
    }
  }
</style>
