<section class="features" id="features">
  <div class="features-container">
    <h2 class="section-title">¿Por qué elegir MOY?</h2>
    <div class="features-grid">
      <div class="feature-card">
        <div class="feature-icon">📐</div>
        <h3>Precisión Perfecta</h3>
        <p>
          Patrones adaptados a tus medidas exactas para un ajuste impecable en
          cada prenda.
        </p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">💰</div>
        <h3>Ahorro de Tela</h3>
        <p>
          Optimización inteligente del trazado que reduce hasta 30% el
          desperdicio de material.
        </p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">🌟</div>
        <h3>Expertos Mundiales</h3>
        <p>
          Diseñadores con experiencia en marcas internacionales crean cada
          patrón.
        </p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">⚡</div>
        <h3>Entrega Rápida</h3>
        <p>Propuesta en 48 horas y patrones finales en tiempo récord.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">🤝</div>
        <h3>Soporte Total</h3>
        <p>Acompañamiento personalizado desde el diseño hasta la producción.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">🔄</div>
        <h3>Ajustes Ilimitados</h3>
        <p>Modificamos hasta lograr tu completa satisfacción.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .features {
    padding: var(--section-padding) 2rem;
    background: var(--white);
  }

  .features-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-title {
    text-align: center;
    font-size: 2.25rem;
    font-weight: 700;
    margin-bottom: 4rem;
    color: var(--dark);
    letter-spacing: -0.01em;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2.5rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .feature-card {
    background: var(--white);
    padding: 2.5rem 2rem;
    border-radius: var(--border-radius-large);
    border: 1px solid var(--gray-lighter);
    transition: all 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-large);
    border-color: var(--primary-light);
  }

  .feature-icon {
    width: 64px;
    height: 64px;
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    color: var(--white);
    border-radius: var(--border-radius);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    margin-bottom: 1.5rem;
    box-shadow: var(--shadow-soft);
  }

  .feature-card h3 {
    font-size: 1.35rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: var(--dark);
    line-height: 1.3;
  }

  .feature-card p {
    color: var(--gray);
    line-height: 1.6;
    font-size: 0.95rem;
  }
</style>
